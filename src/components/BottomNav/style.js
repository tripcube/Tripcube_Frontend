import styled from 'styled-components';

export const BottomNavStyle = styled.div`
  width: 100%;
  height: 44px;
  position: fixed;
  display: flex;
  justify-content: space-around;
  align-items: center;
  bottom: 0px;
  background-color: white;
  filter: drop-shadow(0px -4px 8px rgba(96, 96, 96, 0.25));
  z-index: 100;
`;
